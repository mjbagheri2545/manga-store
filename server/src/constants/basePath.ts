const BASE_PATH = {
  auth: "/auth",
  user: "/users",
  product: "/products",
  chapter: "/chapters",
  tag: "/tags",
  category: "/categories",
  productStatus: "/product-statuses",
} as const;

export default BASE_PATH;
