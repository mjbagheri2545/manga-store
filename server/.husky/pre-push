cd server || exit 1

npm run lint:unusedImports || echo "error in linting" && exit 1
npm run format || echo "error in formatting" && exit 1

cd ../client || exit 1

npm run lint:unusedImports || echo "error in linting" && exit 1
npm run format || echo "error in formatting" && exit 1